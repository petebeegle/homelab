apiVersion: v1
kind: Service
metadata:
  name: snmp-exporter
  namespace: monitoring
  labels:
    app: snmp-exporter
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9116
      targetPort: http
      protocol: TCP
  selector:
    app: snmp-exporter
