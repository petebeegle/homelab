---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: arr-stack-overview
  namespace: grafana
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  folderRef: "arr-stack"
  configMapRef:
    name: arr-stack-dashboard
    key: dashboard.json
