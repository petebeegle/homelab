---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${wildcard_cert_name}
  namespace: gateway
spec:
  secretName: ${wildcard_cert_name}
  dnsNames:
    - "*.${cluster_domain}"
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
