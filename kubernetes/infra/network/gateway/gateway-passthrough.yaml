---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: passthrough
  namespace: gateway
spec:
  gatewayClassName: cilium
  infrastructure:
    annotations:
      io.cilium/lb-ipam-ips: ${gateway_passthrough_ip}
  listeners:
    - protocol: TLS
      port: 443
      name: tls-passthrough
      hostname: "*.${cluster_domain}"
      tls:
        mode: Passthrough
      allowedRoutes:
        namespaces:
          from: All
